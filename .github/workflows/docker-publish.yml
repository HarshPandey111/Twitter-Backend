name: Docker Build and Push

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout code
      - name: Checkout repository
        uses: actions/checkout@v4

<<<<<<< HEAD
      # Step 2: Set up JDK 22 (FIXED!)
      - name: Set up JDK 22
        uses: actions/setup-java@v4
        with:
          java-version: '22'
=======
      # Set up JDK 17
      - name: Set up JDK 22
         uses: actions/setup-java@v4
      with:
         java-version: '22'
>>>>>>> ad21ed9a80a5a9a37efe7ba96ca882737e010e50
          distribution: 'temurin'
          cache: maven

      # Step 3: Build with Maven
      - name: Build with Maven
        run: mvn clean package -DskipTests

      # Step 4: Set up Docker Buildx
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # Step 5: Login to DockerHub
      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # Step 6: Build and push Docker image
      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/twitter-backend:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/twitter-backend:${{ github.sha }}
          cache-from: type=registry,ref=${{ secrets.DOCKERHUB_USERNAME }}/twitter-backend:latest
          cache-to: type=inline

      # Step 7: Success message
      - name: Image pushed successfully
        run: |
          echo "✅ Docker image pushed to DockerHub!"
          echo "🐳 Pull with: docker pull ${{ secrets.DOCKERHUB_USERNAME }}/twitter-backend:latest"
